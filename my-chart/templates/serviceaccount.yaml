{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "gameplay-mcp.serviceAccountName" . }}
  labels:
    app: {{ include "gameplay-mcp.name" . }}
{{- end }}
