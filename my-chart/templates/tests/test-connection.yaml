apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcp-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "mcp-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcp-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
